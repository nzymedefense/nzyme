import React from 'react';

export default function Filters(props) {

  // name, title, allowed filter types [{},{}]
  const fields = props.fields;
  const filters = props.filters;
  const setFilters = props.setFilters;

  return (
      <div className="filters">
        FILTERS
      </div>
  )

}